services:
  memocap:
    build: .
    ports:
      - "8787:8787"
    environment:
      MEMOCAP_DATA_DIR: /data
      MEMOCAP_TOKEN: ${MEMOCAP_TOKEN}
    volumes:
      - memocap-data:/data
    command: ["serve", "--bind", "0.0.0.0:8787"]

volumes:
  memocap-data:
